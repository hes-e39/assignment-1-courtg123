import { Button } from '../generic/Button';
import { Input } from '../generic/Input';

const Countdown = () => {
    return (
        <div>
            <Button>Test</Button>
            <Input
                value="0"
                placeholder="#" />
        </div>
    );
};

export default Countdown;
